{
  "version": 2,
  "builds": [
    { "src": "backend/**/*.js", "use": "@now/node-server" },
    { "src": "frontend/dist/**", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/index.js" },
    { "src": "/(.*)", "dest": "frontend/dist/$1" }
  ]
}

  